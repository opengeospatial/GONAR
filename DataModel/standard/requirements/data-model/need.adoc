[[req_data-model_need]]
[requirement]

====
[%metadata]
identifier:: /req/data-model/need
part:: The implementations of a need SHALL follow the UML model as shown in <<img_need>> with the properties specified in <<tbl_need>> or an extension of it.
====